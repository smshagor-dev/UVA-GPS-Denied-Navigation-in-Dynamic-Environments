# Phase 7 Experiments

Each experiment run should create a metadata document in this directory.

Required fields:

- `experiment_id`
- `run_id`
- `phase`
- `started_at`
- `completed_at`
- `artifact`
- `status`
- `config_snapshots`

The Phase 7 mission runner writes a run record here automatically.

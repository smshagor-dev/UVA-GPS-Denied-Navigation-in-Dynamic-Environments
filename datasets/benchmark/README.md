# Benchmark Dataset Standard

This directory defines the benchmark dataset layout for publication-oriented experiments.

Expected contents:

- dataset manifests
- metadata documents
- benchmark split descriptions
- evaluation references

Use `datasets/benchmark/metadata_schema.json` for metadata validation.

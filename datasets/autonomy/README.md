# Phase 9 Autonomous Dataset Standard

This directory contains the software-only dataset standard for AI autonomy experiments.

Included artifacts:

- `metadata_schema.json`
- `sample_dataset.json`

The sample dataset is synthetic and intended only to validate schema, field naming, and downstream research tooling.

